# AGENTS.md

## Cursor Cloud specific instructions

This is a bare/scaffold repository (`test2`) with no source code, dependencies, or services. The repository contains only a `README.md`.

- **No build, lint, or test commands** are available — there are no `package.json`, `Makefile`, or equivalent files.
- **No services to run.** There are no applications, backends, frontends, databases, or Docker configurations.
- **No dependency installation** is needed. The update script is a no-op (`echo "No dependencies to install"`).

When code is added to this repository, update this section with the relevant build/test/run instructions and any non-obvious caveats discovered during setup.
